import { usePermisos } from "../../hooks/usePermisos.js";  // Se le manda la funcion (permiso) para ver si esta autorizado
import NoPermiso from "../../components/NoPermiso/NoPermiso"; //Retorna Fragmento html para mostrar si no esta autorizado

  const { puede } = usePermisos();
  const puedeVerLogs = puede("getLogs");

  // Hooks SIEMPRE al inicio
  useEffect(() => {
    if (puedeVerLogs) {
      getDatos();
    }
  }, [puedeVerLogs, getDatos]);

  // Ahora sí retornar condicional
  if (!puedeVerLogs) return <NoPermiso />;


onClick={() => { setShowReinitModal(true); setShow(false); }}